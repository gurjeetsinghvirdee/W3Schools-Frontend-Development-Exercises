function addNumbers(a, b) {
    return a + b;
}

let result = addNumbers(379, 745)
console.log(result); // Result 1124



// In this example, addNumbers is the function name, a and b are parameters, 
// and the function returns the sum of a and b. 
// The function is then called with arguments 3 and 5, and the result is logged to the console.