#### (Q Make the function display `"Hello"` in the inner HTML of an element with the ID `"demo"`.

<h5>Solution</h5>

###### Code

```JavaScript

function myFunction() {
    document.getElementById("demo").innerHTML = "Hello";
}

```
